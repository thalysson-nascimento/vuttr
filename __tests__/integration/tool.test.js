import truncate from '../util/truncate';

describe('Tool', () => {
    beforeEach(async () => {
        await truncate();
    });

    it('Deve criar a ferramenta do usuário após ele está devidamente autenticado', async () => {});
});
